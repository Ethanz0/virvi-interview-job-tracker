# virvi-interview-simulator

## Notes
Requirements:
- Setting up firestore backend with `GoogleService-Info.plist`
- `Secrets.plist` file in both widget and main app. Should have structure of GEMINI_API_KEY: string.
